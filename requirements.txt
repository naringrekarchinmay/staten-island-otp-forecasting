# App runtime dependencies (what Streamlit Cloud and CI install).
# The dashboard only needs these; heavy notebook-only libraries
# (prophet, statsmodels, shap, ...) live in requirements-notebooks.txt.
numpy==2.1.3
pandas==2.2.3
plotly==5.24.1
joblib==1.4.2
xgboost==3.2.0
streamlit==1.45.1
