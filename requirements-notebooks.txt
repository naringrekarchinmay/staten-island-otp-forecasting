# Full environment for reproducing the 10 analysis notebooks.
# Installs the app runtime plus the heavy modelling/plotting libraries
# and the Jupyter kernel needed to execute the notebooks.
#
# Two pins carry compatibility constraints:
#   scipy 1.15.3 - scipy>=1.16 removes _lazywhere, breaking statsmodels
#                  0.14.4 (the SARIMA baseline in notebook 07).
#   numpy 2.1.3  - inherited from requirements.txt; numba (a shap
#                  dependency) rejects numpy>2.1, so notebook 05 fails
#                  on a newer numpy.
-r requirements.txt
requests==2.32.3
nbconvert==7.16.6
ipykernel==6.29.5
scipy==1.15.3
scikit-learn==1.6.1
statsmodels==0.14.4
prophet==1.3.0
shap==0.51.0
matplotlib==3.10.0
seaborn==0.13.2
openpyxl==3.1.5
pillow==11.1.0
